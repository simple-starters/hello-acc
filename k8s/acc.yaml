apiVersion: accelerator.tanzu.vmware.com/v1alpha1
kind: Accelerator
metadata:
  name: "acc-name"
  description: |
    acc-description
spec:
  tags:
    - "meta"
  git:
    url: "acc-git-url"
    ref:
      branch: "acc-git-branch"